import { styled } from "styled-components"

const CarouselIndexButtons = styled.div`
    position: absolute;
    left: 50%;
    bottom: 10px;
    transform: translateX(-50%);

    display: flex;
    align-items: end;
    gap: 10px;
`

export default CarouselIndexButtons